# 課題の進め方ガイド

このドキュメントでは、FastAPIプロジェクトの課題に取り組む方法を詳しく説明します。

---

## 📋 課題の全体像

このプロジェクトには、実務レベルのスキルを習得するための以下の課題が含まれています：

### バグ修正課題（2件）
1. タスクモデルのリレーションシップ修正
2. 完了タスク数の集計バグ修正

### 機能追加課題（3件）
3. タスク統計エンドポイント実装
4. 優先度フィルター実装
5. タスク検索機能実装

### メイン課題（1件）⭐
6. カテゴリ管理API実装（サービス層、エンドポイント、テスト）

---

## 🚀 はじめる前に

### 1. 環境構築

まず、アプリケーションを起動できることを確認してください。

**Dockerを使う場合（推奨）**:
```bash
# コンテナを起動
docker-compose up -d

# ログを確認
docker-compose logs -f app

# http://localhost:8000/docs にアクセスしてSwagger UIが表示されることを確認
```

**venv（ローカル環境）を使う場合**:
```bash
# Linux/Mac
./setup.sh
./run_local.sh

# Windows
setup.bat
run_local.bat

# http://localhost:8000/docs にアクセスしてSwagger UIが表示されることを確認
```

### 2. テストが動くことを確認

```bash
# Dockerの場合
docker-compose exec app pytest

# venvの場合
pytest

# 既存のテストがすべてパスすることを確認
```

---

## 📝 課題の進め方（基本フロー）

**重要**: 課題は**必ず1つずつ**進めてください。各課題が完了したら、講師に確認してもらってから次の課題に進みます。

### 各課題の進め方（6ステップ）

#### ステップ1: 課題内容を理解する

このドキュメントとREADME.mdの該当箇所を読み、何を実装すべきか理解します。

```bash
# 例：課題1の場合
# 「課題1: タスクモデルのリレーションシップ修正」のセクションを確認
```

#### ステップ2: 参考コードを確認する

各課題には参考にすべきファイルが記載されています。

```python
# 例：カテゴリAPIを実装する場合
# 参考ファイル: app/services/task_service.py
# 参考ファイル: app/api/tasks.py
```

#### ステップ3: コードを実装する

エディタでファイルを開き、コードを実装します。

```bash
# 例：VSCodeで開く場合
code app/models/task.py
```

#### ステップ4: テストコードを書く（課題2以降）

実装と並行して、テストコードも書きます。

```bash
# 例：完了タスク数のテストを書く
code tests/test_tasks.py
```

#### ステップ5: テストを実行して確認

```bash
# 該当する課題のテストを実行
pytest tests/test_tasks.py::test_get_completed_tasks_count -v

# すべてのテストを実行（既存のテストが壊れていないか確認）
pytest

# テストがすべてパスすることを確認
```

#### ステップ6: Gitにコミットして講師に報告

```bash
# 変更をコミット
git add .
git commit -m "Complete task 1: Fix task model relationship"
git push origin main

# 講師に報告
# 「課題1が完了しました。確認をお願いします。」
```

**⚠️ 講師の確認が完了してから、次の課題に進んでください。**

---

## 🔧 課題別の詳細ガイド

### 課題1: タスクモデルのリレーションシップ修正

**対象ファイル**:
- `app/models/task.py:24`
- `app/models/user.py`

**やること**:
1. `app/models/task.py` の24行目のコメントアウトを解除
2. `app/models/user.py` にも対応するリレーションシップを追加

**ヒント**:
- SQLAlchemyのリレーションシップは双方向で定義が必要
- `back_populates` パラメータを使用

**確認方法**:
```bash
# 既存のテストがすべてパスすることを確認
pytest tests/test_tasks.py -v

# すべてのテストを実行
pytest
```

**完了条件**:
- ✅ すべてのテストがパス
- ✅ コミットしてプッシュ
- ✅ 講師に報告：「課題1が完了しました。確認をお願いします。」

**講師確認ポイント**:
- [ ] `app/models/task.py` にリレーションシップが追加されている
- [ ] `app/models/user.py` にリレーションシップが追加されている
- [ ] `back_populates` が正しく設定されている
- [ ] 既存のテストがすべてパスしている

---

### 課題2: 完了タスク数の集計バグ修正

**⚠️ 課題1の講師確認が完了してから取り組んでください**

**ファイル**: `app/services/task_service.py:69-74`

**対象ファイル**:
- `app/services/task_service.py:69-74`
- `tests/test_tasks.py`（テスト追加）

**やること**:
1. `get_completed_tasks_count` メソッドを修正
2. 完了済み（`is_completed == True`）のタスクのみカウントするようにする
3. `tests/test_tasks.py` にテストコードを追加

**テストの書き方**:
```python
# tests/test_tasks.py の末尾に以下を追加

@pytest.mark.asyncio
async def test_get_completed_tasks_count(client, test_user_data, test_task_data):
    """Test counting only completed tasks"""
    token = await get_auth_token(client, test_user_data)
    headers = {"Authorization": f"Bearer {token}"}

    # 3つのタスクを作成（2つ完了、1つ未完了）
    # ... テストロジックを書く

    # 完了タスク数が2であることを確認
    # assert completed_count == 2
```

**確認方法**:
```bash
# 新しく追加したテストを実行
pytest tests/test_tasks.py::test_get_completed_tasks_count -v

# すべてのテストを実行
pytest
```

**完了条件**:
- ✅ `get_completed_tasks_count` メソッドが修正されている
- ✅ テストコードが追加されている
- ✅ すべてのテストがパス
- ✅ コミットしてプッシュ
- ✅ 講師に報告：「課題2が完了しました。確認をお願いします。」

**講師確認ポイント**:
- [ ] `is_completed == True` のフィルターが追加されている
- [ ] テストが追加されている
- [ ] テストで2完了、1未完了のケースを確認している
- [ ] すべてのテストがパスしている

---

### 課題3: タスク統計エンドポイント実装

**⚠️ 課題2の講師確認が完了してから取り組んでください**

**実装するファイル**:
1. `app/schemas/task.py` - `TaskStats` スキーマを追加
2. `app/services/task_service.py` - `get_task_statistics` メソッドを追加
3. `app/api/tasks.py` - `/api/tasks/statistics` エンドポイントを追加
4. `tests/test_tasks.py` - テストを追加

**期待される動作**:
```bash
# GET /api/tasks/statistics
# レスポンス例:
{
  "total": 10,
  "completed": 6,
  "pending": 4
}
```

**テストの書き方**:
```python
@pytest.mark.asyncio
async def test_task_statistics(client, test_user_data):
    """Test task statistics endpoint"""
    # 5つのタスクを作成（3完了、2未完了）
    # ...

    # 統計情報を取得
    response = await client.get("/api/tasks/statistics", headers=headers)
    assert response.status_code == 200
    data = response.json()
    assert data["total"] == 5
    assert data["completed"] == 3
    assert data["pending"] == 2
```

**確認方法**:
```bash
# 新しく追加したテストを実行
pytest tests/test_tasks.py::test_task_statistics -v
pytest tests/test_tasks.py::test_task_statistics_empty -v

# すべてのテストを実行
pytest

# Swagger UIで確認
# http://localhost:8000/docs の /api/tasks/statistics を試す
```

**完了条件**:
- ✅ `TaskStats` スキーマが追加されている
- ✅ `get_task_statistics` メソッドが実装されている
- ✅ `/api/tasks/statistics` エンドポイントが追加されている
- ✅ テストコードが2つ追加されている
- ✅ すべてのテストがパス
- ✅ Swagger UIで動作確認できる
- ✅ コミットしてプッシュ
- ✅ 講師に報告：「課題3が完了しました。確認をお願いします。」

**講師確認ポイント**:
- [ ] `TaskStats` スキーマが正しく定義されている
- [ ] サービス層で統計情報を正しく集計している
- [ ] エンドポイントが正しく実装されている
- [ ] テストが2つ追加されている（通常ケース、空ケース）
- [ ] すべてのテストがパスしている

---

### 課題4: 優先度フィルター実装

**⚠️ 課題3の講師確認が完了してから取り組んでください**

**実装するファイル**:
1. `app/services/task_service.py` - `get_tasks_by_user` メソッドに `priority` パラメータを追加
2. `app/api/tasks.py` - GET `/api/tasks/` に `priority` クエリパラメータを追加
3. `tests/test_tasks.py` - テストを追加

**期待される動作**:
```bash
# 優先度1のタスクのみ取得
GET /api/tasks/?priority=1

# 優先度3のタスクのみ取得
GET /api/tasks/?priority=3
```

**テストの書き方**:
```python
@pytest.mark.asyncio
async def test_filter_tasks_by_priority(client, test_user_data):
    """Test filtering tasks by priority"""
    # 異なる優先度のタスクを作成
    # Priority 1: 1件
    # Priority 2: 2件
    # Priority 3: 1件

    # 優先度2でフィルター
    response = await client.get("/api/tasks/?priority=2", headers=headers)
    assert len(response.json()) == 2
```

**確認方法**:
```bash
# 新しく追加したテストを実行
pytest tests/test_tasks.py::test_filter_tasks_by_priority -v
pytest tests/test_tasks.py::test_filter_tasks_invalid_priority -v

# すべてのテストを実行
pytest

# Swagger UIで確認
# http://localhost:8000/docs の GET /api/tasks/ で priority パラメータを試す
```

**完了条件**:
- ✅ サービス層に `priority` パラメータが追加されている
- ✅ APIエンドポイントに `priority` クエリパラメータが追加されている
- ✅ テストコードが2つ追加されている
- ✅ すべてのテストがパス
- ✅ Swagger UIで動作確認できる
- ✅ コミットしてプッシュ
- ✅ 講師に報告：「課題4が完了しました。確認をお願いします。」

**講師確認ポイント**:
- [ ] サービス層で優先度フィルターが実装されている
- [ ] クエリパラメータのバリデーション（1-3）が設定されている
- [ ] テストが2つ追加されている（フィルター動作、無効値エラー）
- [ ] すべてのテストがパスしている

---

### 課題5: タスク検索機能実装

**⚠️ 課題4の講師確認が完了してから取り組んでください**

**実装するファイル**:
1. `app/services/task_service.py` - `get_tasks_by_user` メソッドに `search` パラメータを追加
2. `app/api/tasks.py` - GET `/api/tasks/` に `search` クエリパラメータを追加
3. `tests/test_tasks.py` - テストを追加

**期待される動作**:
```bash
# タイトルまたは説明文に "bug" を含むタスクを検索
GET /api/tasks/?search=bug

# 大文字小文字を区別しない
GET /api/tasks/?search=BUG  # 同じ結果
```

**ヒント**:
- SQLAlchemyの `ilike()` を使用（大文字小文字を区別しない）
- `|` 演算子でOR条件（タイトルまたは説明文）

**テストの書き方**:
```python
@pytest.mark.asyncio
async def test_search_tasks(client, test_user_data):
    """Test searching tasks"""
    # 複数のタスクを作成
    # Title: "Fix bug"
    # Title: "Write report", Description: "Bug report"

    # "bug" で検索 → 2件ヒットすることを確認
    response = await client.get("/api/tasks/?search=bug", headers=headers)
    assert len(response.json()) == 2
```

**確認方法**:
```bash
# 新しく追加したテストを実行
pytest tests/test_tasks.py::test_search_tasks -v
pytest tests/test_tasks.py::test_search_tasks_partial_match -v
pytest tests/test_tasks.py::test_combine_priority_and_search -v

# すべてのテストを実行
pytest

# Swagger UIで確認
# http://localhost:8000/docs の GET /api/tasks/ で search パラメータを試す
```

**完了条件**:
- ✅ サービス層に `search` パラメータが追加されている
- ✅ APIエンドポイントに `search` クエリパラメータが追加されている
- ✅ `ilike()` を使った大文字小文字を区別しない検索が実装されている
- ✅ タイトルと説明文の両方を検索できる（OR条件）
- ✅ テストコードが3つ追加されている
- ✅ すべてのテストがパス
- ✅ Swagger UIで動作確認できる
- ✅ コミットしてプッシュ
- ✅ 講師に報告：「課題5が完了しました。確認をお願いします。」

**講師確認ポイント**:
- [ ] `ilike()` を使った検索が実装されている
- [ ] タイトルまたは説明文のOR検索ができる
- [ ] 優先度フィルターと検索の組み合わせができる
- [ ] テストが3つ追加されている
- [ ] すべてのテストがパスしている

---

### 課題6: カテゴリ管理API実装 ⭐（メイン課題）

**⚠️ 課題5の講師確認が完了してから取り組んでください**

これがメインの課題です。タスク管理と同様の機能をゼロから実装します。

**実装するファイル**:
1. `app/services/category_service.py` - 5つのメソッドを実装
2. `app/api/categories.py` - 5つのエンドポイントを実装
3. `app/main.py` - ルーターを登録
4. `tests/test_categories.py` - 9つのテストを実装

#### ステップ1: サービス層の実装

`app/services/category_service.py` を開いて、以下のメソッドを実装：

```python
class CategoryService:
    @staticmethod
    async def get_categories_by_user(db, user_id, skip=0, limit=100):
        """ユーザーのカテゴリ一覧を取得"""
        # 参考: app/services/task_service.py の同名メソッド
        pass

    @staticmethod
    async def get_category_by_id(db, category_id, user_id):
        """カテゴリ詳細を取得"""
        pass

    @staticmethod
    async def create_category(db, category_data, user_id):
        """カテゴリを作成"""
        pass

    @staticmethod
    async def update_category(db, category_id, category_data, user_id):
        """カテゴリを更新"""
        pass

    @staticmethod
    async def delete_category(db, category_id, user_id):
        """カテゴリを削除"""
        pass
```

#### ステップ2: APIエンドポイントの実装

`app/api/categories.py` を開いて、以下のエンドポイントを実装：

```python
@router.get("/", response_model=List[CategoryResponse])
async def get_categories(...):
    """カテゴリ一覧取得"""
    pass

@router.post("/", response_model=CategoryResponse, status_code=201)
async def create_category(...):
    """カテゴリ作成"""
    pass

@router.get("/{category_id}", response_model=CategoryResponse)
async def get_category(...):
    """カテゴリ詳細取得"""
    pass

@router.put("/{category_id}", response_model=CategoryResponse)
async def update_category(...):
    """カテゴリ更新"""
    pass

@router.delete("/{category_id}", status_code=204)
async def delete_category(...):
    """カテゴリ削除"""
    pass
```

**重要**: 認証が必要です。`get_current_active_user` を使用してください。

#### ステップ3: ルーター登録

`app/main.py` を開いて、カテゴリルーターを登録：

```python
from app.api import auth, tasks, users, categories  # categories を追加

# ...

app.include_router(categories.router, prefix="/api")  # この行を追加
```

#### ステップ4: テストの実装

`tests/test_categories.py` を開いて、以下のテストを実装：

**正常系テスト（5件）**:
- `test_create_category` - カテゴリ作成
- `test_get_categories` - カテゴリ一覧取得
- `test_get_category_by_id` - カテゴリ詳細取得
- `test_update_category` - カテゴリ更新
- `test_delete_category` - カテゴリ削除

**異常系テスト（4件）**:
- `test_create_category_without_auth` - 認証なしでエラー
- `test_get_category_not_found` - 存在しないカテゴリで404
- `test_update_category_not_found` - 存在しないカテゴリ更新で404
- `test_delete_category_not_found` - 存在しないカテゴリ削除で404

**テストの書き方**:
```python
@pytest.mark.asyncio
async def test_create_category(client, test_user_data, test_category_data):
    """Test creating a category"""
    # 参考: tests/test_tasks.py の test_create_task

    token = await get_auth_token(client, test_user_data)
    headers = {"Authorization": f"Bearer {token}"}

    response = await client.post(
        "/api/categories/", json=test_category_data, headers=headers
    )
    assert response.status_code == 201
    # ... 他のアサーション
```

#### ステップ5: 動作確認

```bash
# アプリケーションを起動
docker-compose up -d

# Swagger UIにアクセス
# http://localhost:8000/docs

# Categoriesセクションで以下を試す:
# 1. ユーザー登録・ログイン
# 2. Authorizeボタンでトークン設定
# 3. カテゴリ作成
# 4. カテゴリ一覧取得
# 5. カテゴリ更新
# 6. カテゴリ削除

# テストを実行
pytest tests/test_categories.py -v

# すべてパスすることを確認
```

**完了条件**:
- ✅ サービス層の5つのメソッドが実装されている
- ✅ APIエンドポイントの5つが実装されている
- ✅ ルーターが `app/main.py` に登録されている
- ✅ テストコードが9つ実装されている
- ✅ すべてのテストがパス
- ✅ Swagger UIで全エンドポイントが動作確認できる
- ✅ コミットしてプッシュ
- ✅ 講師に報告：「課題6が完了しました。確認をお願いします。」

**講師確認ポイント**:
- [ ] `CategoryService` の5つのメソッドが正しく実装されている
- [ ] `categories.py` の5つのエンドポイントが正しく実装されている
- [ ] 認証処理（`get_current_active_user`）が実装されている
- [ ] ユーザーIDによるデータ分離が実装されている
- [ ] `app/main.py` にルーターが登録されている
- [ ] `tests/test_categories.py` に9つのテストが実装されている
- [ ] すべてのテストがパスしている

---

## ✅ 全課題完了の判定基準

各課題は以下の基準で完了とします：

### 1. テストがすべてパスする

```bash
# すべてのテストを実行
pytest

# または、カバレッジ付きで実行
pytest --cov=app --cov-report=term
```

**すべてのテストがパス（緑色）になればOK！**

### 2. Swagger UIで動作確認できる

- APIドキュメント（http://localhost:8000/docs）で実際にAPIを叩いて動作確認
- 期待通りのレスポンスが返ってくることを確認

### 3. コードが正しく動く

- エラーが出ない
- 期待した結果が返る
- 他人のデータにアクセスできない（セキュリティ）

---

## 🆘 困ったときは

### よくある質問

#### Q1: テストが失敗する
```bash
# エラーメッセージをよく読む
pytest tests/test_categories.py -v

# 特定のテストだけ実行してデバッグ
pytest tests/test_categories.py::test_create_category -v -s
```

#### Q2: データベースエラーが出る
```bash
# データベースを再起動
docker-compose restart db test_db

# または、データベースをクリーンアップ
docker-compose down -v
docker-compose up -d
```

#### Q3: import エラーが出る
```python
# 必要なimportを追加
from typing import List, Optional
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession
```

#### Q4: 認証エラー（401）が出る
```python
# エンドポイントに以下を追加しているか確認
current_user: User = Depends(get_current_active_user),
```

### デバッグのコツ

1. **エラーメッセージを読む**: エラーメッセージには解決のヒントが含まれています
2. **参考コードを見る**: `app/services/task_service.py` や `app/api/tasks.py` を参考に
3. **print デバッグ**: どこまで実行されているか確認
4. **1つずつ実装**: すべて一度に実装せず、1つずつ動作確認

---

## 🎯 チャレンジ課題（ボーナス）

基本課題が終わったら、以下にも挑戦してみてください：

### ボーナス1: カテゴリとタスクのリレーション

**やること**:
1. `Task` モデルに `category_id` フィールドを追加
2. `Category` と `Task` のリレーションシップを構築
3. カテゴリに紐づくタスク一覧を取得するエンドポイントを追加

```python
# GET /api/categories/{category_id}/tasks
# カテゴリに属するタスク一覧を取得
```

### ボーナス2: ページネーションのテスト

大量のデータを作成して、ページネーション（skip、limit）が正しく動作するかテスト

### ボーナス3: カテゴリの色のバリデーション

不正な色コード（例: "#ZZZ"）が拒否されることをテスト

---

## 📚 参考資料

- [FastAPI公式ドキュメント](https://fastapi.tiangolo.com/)
- [SQLAlchemy公式ドキュメント](https://docs.sqlalchemy.org/)
- [pytest公式ドキュメント](https://docs.pytest.org/)
- プロジェクト内の `README.md`
- プロジェクト内の `要件定義書.md`
- プロジェクト内の `設計書.md`

---

## 🎉 全課題完了後の最終確認

すべての課題（課題1〜6）が完了し、講師の確認も完了したら、最終確認を行います。

### 最終確認の手順

#### 1. すべてのテストがパスすることを確認

```bash
# すべてのテストを実行
pytest -v

# カバレッジ付きで実行
pytest --cov=app --cov-report=term

# すべてのテストがパス（緑色）になればOK！
```

#### 2. コードをフォーマット

```bash
# Dockerの場合
make format

# venvの場合
black app tests
isort app tests
```

#### 3. リンターを実行

```bash
# Dockerの場合
make lint

# venvの場合
flake8 app tests
mypy app
```

#### 4. 最終コミット

```bash
git add .
git commit -m "Complete all assignments (Task 1-6)"
git push origin main
```

#### 5. 講師に最終報告

「全課題が完了しました。最終確認をお願いします。」

**おつかれさまでした！🎉**

---

**頑張ってください！実践的なスキルが身につきます！** 🚀
