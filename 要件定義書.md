# 要件定義書

## 1. プロジェクト概要

### 1.1 プロジェクト名
FastAPI タスク管理アプリケーション

### 1.2 目的
個人ユーザー向けのタスク管理機能を提供するREST APIアプリケーションの開発

### 1.3 対象ユーザー
- 個人でタスク管理を行いたいユーザー
- タスクの優先度管理や分類整理を必要とするユーザー
- 学習目的でFastAPIの実装を学びたい開発者

### 1.4 開発期間・環境
- 開発言語: Python 3.11以上
- フレームワーク: FastAPI 0.109.0
- データベース: PostgreSQL 15
- 実行環境: Docker / ローカル開発環境(venv)

---

## 2. 機能要件

### 2.1 認証機能

#### 2.1.1 ユーザー登録
- **機能ID**: AUTH-001
- **優先度**: 高
- **概要**: 新規ユーザーのアカウント作成機能
- **詳細要件**:
  - メールアドレス、ユーザー名、パスワードを入力して登録
  - メールアドレスとユーザー名の重複チェック
  - パスワードのハッシュ化（bcrypt使用）
  - 登録時にユーザーアカウントはアクティブ状態で作成
- **入力項目**:
  - email (必須、文字列、一意)
  - username (必須、文字列、一意)
  - password (必須、文字列、最小8文字)
- **出力**: ユーザー情報（パスワードを除く）
- **エラーケース**:
  - メールアドレス重複時: 400 Bad Request
  - バリデーションエラー: 422 Unprocessable Entity

#### 2.1.2 ログイン
- **機能ID**: AUTH-002
- **優先度**: 高
- **概要**: ユーザー認証とアクセストークン発行
- **詳細要件**:
  - メールアドレス（またはユーザー名）とパスワードで認証
  - JWT形式のアクセストークンを発行
  - トークン有効期限: 30分（デフォルト）
- **入力項目**:
  - username (メールアドレス)
  - password
- **出力**:
  - access_token (JWT)
  - token_type: "bearer"
- **エラーケース**:
  - 認証失敗時: 401 Unauthorized
  - アカウント無効時: 401 Unauthorized

### 2.2 ユーザー管理機能

#### 2.2.1 現在のユーザー情報取得
- **機能ID**: USER-001
- **優先度**: 高
- **概要**: ログイン中のユーザー自身の情報を取得
- **認証**: 必須（JWT Bearer Token）
- **出力**: ユーザー情報（ID、email、username、is_active、作成日時、更新日時）

#### 2.2.2 ユーザー情報更新
- **機能ID**: USER-002
- **優先度**: 中
- **概要**: ログイン中のユーザー自身の情報を更新
- **認証**: 必須（JWT Bearer Token）
- **更新可能項目**:
  - email（一意性制約あり）
  - username（一意性制約あり）
- **エラーケース**:
  - メールアドレス/ユーザー名重複時: 400 Bad Request

#### 2.2.3 特定ユーザー情報取得
- **機能ID**: USER-003
- **優先度**: 低
- **概要**: 指定したユーザーIDのユーザー情報を取得
- **認証**: 必須（JWT Bearer Token）
- **エラーケース**:
  - ユーザーが存在しない場合: 404 Not Found

### 2.3 タスク管理機能

#### 2.3.1 タスク一覧取得
- **機能ID**: TASK-001
- **優先度**: 高
- **概要**: ログインユーザーのタスク一覧を取得
- **認証**: 必須（JWT Bearer Token）
- **詳細要件**:
  - ユーザー自身のタスクのみ取得可能
  - ページネーション対応（skip、limit）
  - デフォルト: 最大100件
- **クエリパラメータ**:
  - skip: オフセット（デフォルト: 0）
  - limit: 取得件数（1-100、デフォルト: 100）
- **出力**: タスク情報の配列

#### 2.3.2 タスク作成
- **機能ID**: TASK-002
- **優先度**: 高
- **概要**: 新規タスクの作成
- **認証**: 必須（JWT Bearer Token）
- **入力項目**:
  - title (必須、最大200文字)
  - description (任意、テキスト)
  - priority (任意、1-3の整数、デフォルト: 1)
    - 1: Low（低）
    - 2: Medium（中）
    - 3: High（高）
- **自動設定項目**:
  - user_id: ログインユーザーのID
  - is_completed: false（未完了）
  - created_at: 作成日時
  - updated_at: 更新日時
- **出力**: 作成されたタスク情報
- **ステータスコード**: 201 Created

#### 2.3.3 タスク詳細取得
- **機能ID**: TASK-003
- **優先度**: 高
- **概要**: 指定したタスクIDのタスク詳細を取得
- **認証**: 必須（JWT Bearer Token）
- **詳細要件**:
  - 自分が作成したタスクのみ取得可能
- **エラーケース**:
  - タスクが存在しない、または他ユーザーのタスク: 404 Not Found

#### 2.3.4 タスク更新
- **機能ID**: TASK-004
- **優先度**: 高
- **概要**: 既存タスクの情報を更新
- **認証**: 必須（JWT Bearer Token）
- **更新可能項目**:
  - title (任意、最大200文字)
  - description (任意、テキスト)
  - is_completed (任意、真偽値)
  - priority (任意、1-3の整数)
- **詳細要件**:
  - 自分が作成したタスクのみ更新可能
  - 部分更新対応（指定した項目のみ更新）
  - updated_atは自動更新
- **エラーケース**:
  - タスクが存在しない、または他ユーザーのタスク: 404 Not Found

#### 2.3.5 タスク削除
- **機能ID**: TASK-005
- **優先度**: 高
- **概要**: 既存タスクの削除
- **認証**: 必須（JWT Bearer Token）
- **詳細要件**:
  - 自分が作成したタスクのみ削除可能
  - 物理削除（データベースから完全削除）
- **ステータスコード**: 204 No Content
- **エラーケース**:
  - タスクが存在しない、または他ユーザーのタスク: 404 Not Found

### 2.4 カテゴリ管理機能（実装課題）

#### 2.4.1 カテゴリ一覧取得
- **機能ID**: CATEGORY-001
- **優先度**: 中
- **概要**: ログインユーザーのカテゴリ一覧を取得
- **認証**: 必須（JWT Bearer Token）
- **詳細要件**:
  - ユーザー自身のカテゴリのみ取得可能
  - ページネーション対応（skip、limit）
- **ステータス**: 未実装（学習課題）

#### 2.4.2 カテゴリ作成
- **機能ID**: CATEGORY-002
- **優先度**: 中
- **概要**: 新規カテゴリの作成
- **認証**: 必須（JWT Bearer Token）
- **入力項目**:
  - name (必須、最大100文字、一意)
  - description (任意、テキスト)
  - color (任意、hex color code、例: #FF5733)
- **ステータス**: 未実装（学習課題）

#### 2.4.3 カテゴリ詳細取得
- **機能ID**: CATEGORY-003
- **優先度**: 中
- **ステータス**: 未実装（学習課題）

#### 2.4.4 カテゴリ更新
- **機能ID**: CATEGORY-004
- **優先度**: 中
- **ステータス**: 未実装（学習課題）

#### 2.4.5 カテゴリ削除
- **機能ID**: CATEGORY-005
- **優先度**: 中
- **ステータス**: 未実装（学習課題）

### 2.5 追加機能要件（未実装・拡張課題）

#### 2.5.1 タスク統計情報取得
- **機能ID**: TASK-006
- **優先度**: 低
- **概要**: ユーザーのタスク統計情報を取得
- **出力項目**:
  - total: 総タスク数
  - completed: 完了タスク数
  - pending: 未完了タスク数
- **ステータス**: 未実装（学習課題）

#### 2.5.2 優先度別タスクフィルター
- **機能ID**: TASK-007
- **優先度**: 低
- **概要**: 優先度を指定してタスクを絞り込み
- **クエリパラメータ**: priority (1-3)
- **ステータス**: 未実装（学習課題）

#### 2.5.3 タスク検索
- **機能ID**: TASK-008
- **優先度**: 低
- **概要**: タイトルや説明文でタスクを検索
- **クエリパラメータ**: query (検索文字列)
- **ステータス**: 未実装（学習課題）

---

## 3. 非機能要件

### 3.1 性能要件
- **レスポンスタイム**: 通常のAPI呼び出しで1秒以内
- **同時接続数**: 開発環境では10ユーザー程度を想定
- **データベース接続**: 非同期処理対応（SQLAlchemy async）

### 3.2 セキュリティ要件
- **認証方式**: JWT (JSON Web Token) による Bearer Token認証
- **パスワード保存**: bcryptによるハッシュ化（平文保存禁止）
- **アルゴリズム**: HS256
- **トークン有効期限**: 30分（設定変更可能）
- **HTTPS**: 本番環境では必須（現状は開発環境のみ）
- **CORS**: 本番環境では適切なオリジン制限が必要（現状は`*`で全許可）

### 3.3 可用性要件
- **稼働率**: 開発環境では特に規定なし
- **データバックアップ**: PostgreSQLのバックアップ機能に依存

### 3.4 拡張性要件
- **データベース**: PostgreSQL使用、スケーラブルなRDBMS
- **ORM**: SQLAlchemy 2.0（非同期対応）
- **マイグレーション**: Alembic対応（将来的なスキーマ変更に対応）

### 3.5 保守性要件
- **コード品質**:
  - Linter: flake8によるコードチェック
  - Formatter: blackによる自動フォーマット
  - 型チェック: mypyによる静的型チェック
  - Import整理: isortによるimport文の整理
- **テスト**:
  - ユニットテスト: pytest
  - テストカバレッジ: pytest-cov（目標カバレッジ率は規定なし）
  - 非同期テスト: pytest-asyncio
- **ドキュメント**:
  - API仕様書: FastAPIの自動生成ドキュメント（Swagger UI、ReDoc）
  - コメント: 日本語でのインラインコメント推奨

### 3.6 互換性要件
- **Python**: 3.11以上
- **データベース**: PostgreSQL 15以上
- **実行環境**:
  - Docker環境（推奨）
  - ローカル環境（Windows/Mac/Linux）

### 3.7 運用要件
- **ログ**: 標準出力へのログ出力（uvicornのデフォルト設定）
- **監視**: ヘルスチェックエンドポイント `/health` の提供
- **環境設定**: `.env`ファイルによる環境変数管理

---

## 4. データ要件

### 4.1 データモデル

#### 4.1.1 Users（ユーザー）
| カラム名 | データ型 | 制約 | 説明 |
|---------|---------|------|------|
| id | Integer | PRIMARY KEY, AUTO INCREMENT | ユーザーID |
| email | String | UNIQUE, NOT NULL, INDEX | メールアドレス |
| username | String | UNIQUE, NOT NULL, INDEX | ユーザー名 |
| hashed_password | String | NOT NULL | ハッシュ化パスワード |
| is_active | Boolean | DEFAULT true | アカウント有効フラグ |
| created_at | DateTime | DEFAULT now() | 作成日時 |
| updated_at | DateTime | DEFAULT now(), ON UPDATE now() | 更新日時 |

#### 4.1.2 Tasks（タスク）
| カラム名 | データ型 | 制約 | 説明 |
|---------|---------|------|------|
| id | Integer | PRIMARY KEY, AUTO INCREMENT | タスクID |
| title | String(200) | NOT NULL | タスクタイトル |
| description | Text | NULLABLE | タスク説明 |
| is_completed | Boolean | DEFAULT false | 完了フラグ |
| priority | Integer | DEFAULT 1 | 優先度（1-3） |
| user_id | Integer | FOREIGN KEY(users.id), NOT NULL | ユーザーID |
| created_at | DateTime | DEFAULT now() | 作成日時 |
| updated_at | DateTime | DEFAULT now(), ON UPDATE now() | 更新日時 |

#### 4.1.3 Categories（カテゴリ）
| カラム名 | データ型 | 制約 | 説明 |
|---------|---------|------|------|
| id | Integer | PRIMARY KEY, AUTO INCREMENT | カテゴリID |
| name | String(100) | UNIQUE, NOT NULL | カテゴリ名 |
| description | Text | NULLABLE | カテゴリ説明 |
| color | String(7) | NULLABLE | カラーコード（例: #FF5733） |
| user_id | Integer | FOREIGN KEY(users.id), NOT NULL | ユーザーID |
| created_at | DateTime | DEFAULT now() | 作成日時 |
| updated_at | DateTime | DEFAULT now(), ON UPDATE now() | 更新日時 |

### 4.2 データ関連
- **Users - Tasks**: 1対多（1ユーザーは複数のタスクを持つ）
- **Users - Categories**: 1対多（1ユーザーは複数のカテゴリを持つ）
- **Categories - Tasks**: 1対多（将来の拡張、現状未実装）

### 4.3 データ保持期間
- **ユーザーデータ**: 削除されるまで永続保持
- **タスクデータ**: 削除されるまで永続保持
- **カテゴリデータ**: 削除されるまで永続保持

### 4.4 データバックアップ
- PostgreSQLのバックアップ機能を使用
- バックアップ頻度: 運用環境に依存（開発環境では規定なし）

---

## 5. インターフェース要件

### 5.1 API仕様

#### 5.1.1 APIエンドポイント一覧

**認証API**
- `POST /api/auth/register` - ユーザー登録
- `POST /api/auth/login` - ログイン

**ユーザーAPI**
- `GET /api/users/me` - 現在のユーザー情報取得
- `PUT /api/users/me` - 現在のユーザー情報更新
- `GET /api/users/{user_id}` - ユーザー情報取得

**タスクAPI**
- `GET /api/tasks/` - タスク一覧取得
- `POST /api/tasks/` - タスク作成
- `GET /api/tasks/{task_id}` - タスク詳細取得
- `PUT /api/tasks/{task_id}` - タスク更新
- `DELETE /api/tasks/{task_id}` - タスク削除

**カテゴリAPI**（未実装）
- `GET /api/categories/` - カテゴリ一覧取得
- `POST /api/categories/` - カテゴリ作成
- `GET /api/categories/{category_id}` - カテゴリ詳細取得
- `PUT /api/categories/{category_id}` - カテゴリ更新
- `DELETE /api/categories/{category_id}` - カテゴリ削除

**その他**
- `GET /` - ルートエンドポイント（アプリ情報）
- `GET /health` - ヘルスチェック
- `GET /docs` - Swagger UI（API仕様書）
- `GET /redoc` - ReDoc（API仕様書）

#### 5.1.2 リクエスト/レスポンス形式
- **Content-Type**: `application/json`
- **文字エンコーディング**: UTF-8
- **認証ヘッダー**: `Authorization: Bearer {access_token}`

#### 5.1.3 HTTPステータスコード
- `200 OK`: 正常な取得・更新
- `201 Created`: 正常な作成
- `204 No Content`: 正常な削除
- `400 Bad Request`: リクエストエラー（重複、バリデーションエラー等）
- `401 Unauthorized`: 認証エラー
- `404 Not Found`: リソース未発見
- `422 Unprocessable Entity`: バリデーションエラー
- `500 Internal Server Error`: サーバーエラー

### 5.2 外部システム連携
- 現時点で外部システムとの連携なし

---

## 6. 制約事項

### 6.1 技術的制約
- Python 3.11以上が必要
- PostgreSQL 15以上が必要
- Docker使用時はDocker Desktopまたは互換環境が必要

### 6.2 運用上の制約
- 本番環境での運用は想定していない（学習・開発目的）
- マルチテナント対応なし（ユーザー間のデータ分離のみ）

### 6.3 既知の問題・バグ
以下は学習課題として意図的に残されている問題：

1. **タスクモデルのリレーションシップ** (`app/models/task.py:24`)
   - Userモデルとのリレーションシップがコメントアウトされている

2. **完了タスク数の集計バグ** (`app/services/task_service.py:64`)
   - `get_completed_tasks_count`メソッドが全タスクをカウントしている

3. **CORS設定** (`app/main.py:31`)
   - 全オリジンを許可しているため、本番環境では不適切

---

## 7. テスト要件

### 7.1 テスト範囲
- 認証機能のテスト（登録、ログイン）
- ユーザー管理機能のテスト（CRUD）
- タスク管理機能のテスト（CRUD）
- カテゴリ管理機能のテスト（未実装課題）

### 7.2 テストツール
- **フレームワーク**: pytest
- **非同期テスト**: pytest-asyncio
- **カバレッジ**: pytest-cov
- **HTTPクライアント**: httpx（TestClient用）

### 7.3 テストデータ
- テスト用データベース: `test_db`
- テストごとにデータベースを初期化（conftest.pyで管理）
- フィクスチャによるテストユーザー作成

### 7.4 テスト実施方法
```bash
# Docker環境
make test

# venv環境
pytest
```

---

## 8. 移行・展開要件

### 8.1 初期データ
- 初回起動時にテーブルが自動作成される（SQLAlchemyのcreate_all）
- 初期ユーザーやマスターデータの投入は不要

### 8.2 マイグレーション
- Alembic対応（現状未使用）
- 将来的なスキーマ変更に対応可能

### 8.3 環境設定
- `.env`ファイルによる環境変数設定
- 必須環境変数:
  - `DATABASE_URL`: 本番DB接続URL
  - `TEST_DATABASE_URL`: テストDB接続URL
  - `SECRET_KEY`: JWT署名用シークレットキー

---

## 9. 用語集

| 用語 | 説明 |
|------|------|
| JWT | JSON Web Token。認証情報を含むトークン形式 |
| Bearer Token | HTTPヘッダーに含める認証トークンの形式 |
| bcrypt | パスワードのハッシュ化アルゴリズム |
| ORM | Object-Relational Mapping。SQLAlchemyが該当 |
| CRUD | Create, Read, Update, Deleteの略 |
| CORS | Cross-Origin Resource Sharing。異なるオリジン間のリソース共有 |
| Async/Await | Pythonの非同期処理構文 |
| Pydantic | Pythonのデータバリデーションライブラリ |
| FastAPI | PythonのモダンなWebフレームワーク |

---

## 10. 参考資料

- [FastAPI公式ドキュメント](https://fastapi.tiangolo.com/)
- [SQLAlchemy公式ドキュメント](https://docs.sqlalchemy.org/)
- [Pydantic公式ドキュメント](https://docs.pydantic.dev/)
- [pytest公式ドキュメント](https://docs.pytest.org/)

---

## 11. 変更履歴

| 版数 | 日付 | 変更内容 | 作成者 |
|------|------|----------|--------|
| 1.0 | 2025-11-22 | 初版作成 | - |

---

**文書管理情報**
- 文書番号: REQ-001
- 承認状態: Draft（ドラフト）
- 最終更新日: 2025-11-22
