# 整合性チェックレポート

**プロジェクト**: FastAPI タスク管理アプリケーション
**チェック日時**: 2025-11-22
**対象**: コードベース vs 要件定義書・設計書

---

## 1. エグゼクティブサマリー

リポジトリ全体と要件定義書・設計書の整合性を確認した結果、以下の不整合が発見されました。

### 総合評価: ⚠️ 軽微な不整合あり

- **重大な不整合**: 0件
- **中程度の不整合**: 4件
- **軽微な不整合**: 3件
- **ドキュメント誤記**: 2件

---

## 2. 発見された不整合

### 2.1 中程度の不整合

#### 🔴 [MEDIUM-01] データベースセッションファクトリの名前不一致

**場所**: `app/core/database.py:11`

**要件定義書・設計書での記載**:
```python
async_session_maker = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
```

**実際のコード**:
```python
async_session = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
```

**影響**:
- 設計書の説明と実装が異なる
- 変数名が `async_session` であり、`async_session_maker` ではない

**推奨対応**:
- 設計書を実コードに合わせて修正する（変数名を `async_session` に変更）
- または、コードを設計書に合わせて `async_session_maker` にリネームする

**優先度**: 中

---

#### 🔴 [MEDIUM-02] security.pyの関数名不一致

**場所**: `app/core/security.py:35`

**設計書での記載**:
- 関数名: `verify_token(token: str) -> str`

**実際のコード**:
```python
def decode_access_token(token: str) -> Optional[str]:
```

**影響**:
- 設計書の関数名と実装が異なる
- 戻り値の型も異なる（`str` vs `Optional[str]`）

**推奨対応**:
- 設計書を修正して関数名を `decode_access_token` に変更
- 戻り値の型も `Optional[str]` に修正

**優先度**: 中

---

#### 🔴 [MEDIUM-03] UserUpdateスキーマに余分なフィールド

**場所**: `app/schemas/user.py:20-26`

**要件定義書での記載**:
- 更新可能項目: `email`, `username` のみ

**実際のコード**:
```python
class UserUpdate(BaseModel):
    email: Optional[EmailStr] = None
    username: Optional[str] = None
    password: Optional[str] = None      # ← 要件定義書に記載なし
    is_active: Optional[bool] = None    # ← 要件定義書に記載なし
```

**影響**:
- 要件定義書に記載されていないフィールドが実装されている
- `password` の更新は合理的だが、`is_active` の更新は通常管理者権限が必要

**推奨対応**:
- 要件定義書に `password` 更新機能を追加記載
- `is_active` フィールドの更新について、セキュリティ上の考慮が必要か検討
- または、`UserUpdate` から `is_active` を削除

**優先度**: 中

---

#### 🔴 [MEDIUM-04] Categoryモデルのnameフィールドのユニーク制約

**場所**: `app/models/category.py:15`

**要件定義書での記載**:
- `name` カラム: UNIQUE制約あり

**実際のコード**:
```python
name = Column(String(100), nullable=False, unique=True)
```

**問題点**:
- グローバルでユニーク制約がかかっている
- 異なるユーザー間で同じカテゴリ名が使えない（例: ユーザーA, Bの両方が「仕事」カテゴリを作れない）

**推奨対応**:
- `unique=True` を削除
- または、複合ユニーク制約 `UniqueConstraint('name', 'user_id')` を追加

**優先度**: 中（実装課題のため、学習者が修正すべき意図的なバグの可能性あり）

---

### 2.2 軽微な不整合

#### 🟡 [MINOR-01] migrationsディレクトリが空

**場所**: `/migrations/`

**要件定義書・設計書での記載**:
- Alembic対応（将来的なスキーマ変更に対応）

**実際の状況**:
- `migrations/` ディレクトリは存在するが空
- Alembicの初期化がされていない

**影響**:
- 現状は `Base.metadata.create_all()` でテーブル作成している
- マイグレーション機能は未実装

**推奨対応**:
- 現状維持（学習プロジェクトのため）
- または、Alembicを初期化（`alembic init migrations`）

**優先度**: 低

---

#### 🟡 [MINOR-02] カテゴリルーターがmain.pyに未登録

**場所**: `app/main.py:38-40`

**設計書での記載**:
- `app.include_router(categories.router, prefix="/api")` を追加する必要がある

**実際のコード**:
```python
app.include_router(auth.router, prefix="/api")
app.include_router(users.router, prefix="/api")
app.include_router(tasks.router, prefix="/api")
# categories.router は未登録
```

**影響**:
- カテゴリAPIエンドポイントが有効化されていない
- これは意図的な未実装（学習課題）

**推奨対応**:
- 現状維持（学習課題として適切）
- README.mdに「実装後に `main.py` でルーター登録が必要」と明記済み

**優先度**: 低（意図的な未実装）

---

#### 🟡 [MINOR-03] TODOコメントの行番号のずれ

**場所**: 各所

**設計書での記載**:
- `app/api/tasks.py:75-77`: TODOコメント

**実際のコード**:
- `app/api/tasks.py:77-79`: TODOコメント

**影響**:
- 設計書の行番号が実際のコードと2行ずれている

**推奨対応**:
- 設計書の行番号を修正（77-79に変更）

**優先度**: 低

---

### 2.3 ドキュメント誤記

#### 📝 [DOC-01] 設計書のディレクトリ構成の誤記

**場所**: 設計書 2.2節

**設計書での記載**:
```
├── migrations/                    # DBマイグレーション（Alembic - 未使用）
```

**実際の状況**:
- ディレクトリは存在するが完全に空

**推奨対応**:
- 「未使用」を「未設定（空ディレクトリ）」に修正

**優先度**: 低

---

#### 📝 [DOC-02] 設計書のデータベース設計の誤記

**場所**: 設計書 6.2節

**設計書での記載**:
```python
async_session_maker = sessionmaker(...)
```

**実際のコード**:
```python
async_session = sessionmaker(...)
```

**推奨対応**:
- 設計書を実コードに合わせて修正

**優先度**: 低

---

## 3. 正しく実装されている項目

以下の項目は要件定義書・設計書と完全に一致していることを確認しました。

### ✅ 3.1 データモデル
- **User Model**: 完全一致
- **Task Model**: 完全一致（意図的なバグを除く）
- **Category Model**: ほぼ一致（UNIQUE制約の問題を除く）

### ✅ 3.2 スキーマ定義
- **UserCreate, UserResponse**: 完全一致
- **TaskBase, TaskCreate, TaskUpdate, TaskResponse**: 完全一致
- **CategoryBase, CategoryCreate, CategoryUpdate, CategoryResponse**: 完全一致

### ✅ 3.3 APIエンドポイント
- **認証API** (`auth.py`): 完全一致
- **ユーザーAPI** (`users.py`): 完全一致
- **タスクAPI** (`tasks.py`): 完全一致
- **カテゴリAPI** (`categories.py`): 意図的な未実装（学習課題）

### ✅ 3.4 サービス層
- **UserService**: 完全一致
- **TaskService**: 完全一致（意図的なバグを除く）
- **CategoryService**: 意図的な未実装（学習課題）

### ✅ 3.5 セキュリティ
- **JWT認証**: 実装完了、仕様通り
- **パスワードハッシュ**: bcrypt使用、仕様通り
- **CORS設定**: 設計書通り（開発環境設定）

### ✅ 3.6 Docker環境
- **docker-compose.yml**: 設計書通り
- **Dockerfile**: 設計書通り
- **サービス構成**: app, db, test_dbの3サービス構成

### ✅ 3.7 テスト環境
- **pytest設定**: `pyproject.toml` で正しく設定
- **フィクスチャ**: conftest.pyで実装済み
- **テストファイル**: test_auth.py, test_users.py, test_tasks.py 実装済み

### ✅ 3.8 開発ツール
- **Makefile**: 設計書通りのコマンド提供
- **black, flake8, mypy, isort**: 設定ファイル完備
- **環境変数管理**: .env, .env.example, .env.local 完備

---

## 4. 意図的な未実装・バグ（学習課題）

以下は学習課題として意図的に残されているため、不整合ではありません。

### 📚 4.1 学習課題として確認済み

#### バグ修正課題
1. ✅ **タスクモデルのリレーションシップ** (`app/models/task.py:24`)
   - コメントアウト状態を確認
   - README.md、設計書に明記済み

2. ✅ **完了タスク数の集計バグ** (`app/services/task_service.py:71-74`)
   - バグの存在を確認
   - コメントで「BUG:」と明記済み

#### 機能追加課題
3. ✅ **カテゴリ管理API** (`app/api/categories.py`, `app/services/category_service.py`)
   - 未実装を確認
   - TODOコメントで実装指示あり

4. ✅ **タスク統計エンドポイント** (`app/api/tasks.py:77`)
   - 未実装を確認
   - TODOコメントあり

5. ✅ **優先度フィルター** (`app/api/tasks.py:78`)
   - 未実装を確認
   - TODOコメントあり

6. ✅ **タスク検索機能** (`app/api/tasks.py:79`)
   - 未実装を確認
   - TODOコメントあり

---

## 5. セキュリティ上の懸念事項

### 🔒 5.1 CORS設定

**場所**: `app/main.py:31`

**現在の設定**:
```python
allow_origins=["*"]  # 全オリジン許可
```

**状況**:
- 要件定義書・設計書で「開発環境のみ」と明記済み
- TODOコメントで本番環境での変更を促している

**推奨対応**:
- 現状維持（学習プロジェクトのため）
- 本番デプロイ時は必ず修正

**優先度**: 低（学習環境のため問題なし）

---

### 🔒 5.2 UserUpdate の is_active フィールド

**場所**: `app/schemas/user.py:26`

**懸念事項**:
- 一般ユーザーが自分のアカウントを無効化できる可能性
- 要件定義書に記載なし

**推奨対応**:
- 要件定義書に記載を追加
- または、管理者専用エンドポイントに分離

**優先度**: 中

---

## 6. 推奨される修正事項

### 優先度: 高
該当なし

### 優先度: 中
1. [MEDIUM-01] データベースセッション変数名を統一
2. [MEDIUM-02] security.pyの関数名を設計書と統一
3. [MEDIUM-03] UserUpdateスキーマのフィールドを要件定義書に記載
4. [MEDIUM-04] Categoryモデルのユニーク制約を修正

### 優先度: 低
1. [MINOR-01] Alembicの初期化（任意）
2. [MINOR-03] 設計書の行番号を修正
3. [DOC-01] 設計書のディレクトリ説明を修正
4. [DOC-02] 設計書のコード例を修正

---

## 7. 修正案

### 7.1 設計書の修正

#### 修正箇所1: 6.1.2節 database.py

**修正前**:
```python
async_session_maker = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
```

**修正後**:
```python
async_session = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
```

---

#### 修正箇所2: 6.1.3節 security.py

**修正前**:
```
主要関数:
- verify_token(token: str) -> str: JWT検証
```

**修正後**:
```
主要関数:
- decode_access_token(token: str) -> Optional[str]: JWT検証
```

---

#### 修正箇所3: 14.1節 既知の課題・TODO

**追加事項**:
```
### 14.3 設計上の検討事項
1. Categoryモデルのname フィールドのユニーク制約
   - 現状: グローバルユニーク（全ユーザーで名前が重複不可）
   - 推奨: ユーザーごとのユニーク制約（user_id との複合ユニーク制約）
```

---

### 7.2 要件定義書の修正

#### 修正箇所1: 2.2.2節 ユーザー情報更新

**修正前**:
```
更新可能項目:
- email（一意性制約あり）
- username（一意性制約あり）
```

**修正後**:
```
更新可能項目:
- email（一意性制約あり）
- username（一意性制約あり）
- password（パスワード変更）
- is_active（アカウント有効化/無効化フラグ）
```

---

#### 修正箇所2: 3.2節 セキュリティ要件

**追加事項**:
```
### 3.2.5 アカウント無効化
- ユーザー自身がアカウントを無効化できる（is_activeフラグ）
- 無効化されたアカウントは認証に失敗する
```

---

### 7.3 コードの修正案（任意）

#### 修正案1: app/models/category.py

**現在のコード**:
```python
name = Column(String(100), nullable=False, unique=True)
```

**修正案**:
```python
from sqlalchemy import UniqueConstraint

class Category(Base):
    __tablename__ = "categories"

    id = Column(Integer, primary_key=True, index=True)
    name = Column(String(100), nullable=False)
    description = Column(Text, nullable=True)
    color = Column(String(7), nullable=True)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

    __table_args__ = (
        UniqueConstraint('name', 'user_id', name='uq_category_name_user'),
    )
```

---

## 8. 結論

### 8.1 総合評価

プロジェクト全体として、要件定義書・設計書との整合性は**高い水準**にあります。

- ✅ **アーキテクチャ**: 設計書通りに実装
- ✅ **データモデル**: 要件定義書通りに実装
- ✅ **API設計**: 仕様通りに実装
- ✅ **セキュリティ**: JWT認証、bcrypt実装済み
- ✅ **テスト環境**: pytest設定完備
- ⚠️ **ドキュメント**: 軽微な誤記あり

### 8.2 不整合の性質

発見された不整合の多くは以下のいずれかです:

1. **ドキュメントの誤記**: コードは正しいが、ドキュメントの説明が古い
2. **変数名の不一致**: 機能的には問題ないが、名前が異なる
3. **意図的な未実装**: 学習課題として残されている

### 8.3 推奨アクション

#### 即座に対応すべき項目
1. Categoryモデルのユニーク制約の見直し（MEDIUM-04）
2. UserUpdateスキーマのフィールドを要件定義書に追記（MEDIUM-03）

#### 時間があれば対応する項目
1. 設計書の変数名・関数名を実コードに合わせて修正
2. 行番号のずれを修正

#### 対応不要な項目
1. 意図的な未実装（学習課題）
2. migrationsディレクトリの空状態（現状は create_all で対応）

---

## 9. チェック対象ファイル一覧

以下のファイルを確認しました:

### コアファイル
- ✅ `app/main.py`
- ✅ `app/core/config.py`
- ✅ `app/core/database.py`
- ✅ `app/core/security.py`

### モデル
- ✅ `app/models/user.py`
- ✅ `app/models/task.py`
- ✅ `app/models/category.py`

### スキーマ
- ✅ `app/schemas/user.py`
- ✅ `app/schemas/task.py`
- ✅ `app/schemas/category.py`

### サービス
- ✅ `app/services/user_service.py`
- ✅ `app/services/task_service.py`
- ✅ `app/services/category_service.py`

### API
- ✅ `app/api/deps.py`
- ✅ `app/api/auth.py`
- ✅ `app/api/users.py`
- ✅ `app/api/tasks.py`
- ✅ `app/api/categories.py`

### インフラ・設定
- ✅ `docker-compose.yml`
- ✅ `Dockerfile`
- ✅ `requirements.txt`
- ✅ `pyproject.toml`
- ✅ `Makefile`

### ドキュメント
- ✅ `README.md`
- ✅ `要件定義書.md`
- ✅ `設計書.md`

---

**チェック完了日**: 2025-11-22
**チェック担当**: 自動整合性チェック
**次回チェック推奨**: コード修正後、またはドキュメント更新後
