# 設計書

## 1. システム概要

### 1.1 システム名
FastAPI タスク管理アプリケーション

### 1.2 システム概要
個人ユーザー向けのタスク管理機能を提供するREST APIシステム。ユーザー認証、タスクのCRUD操作、カテゴリ管理機能を備えた非同期Webアプリケーション。

### 1.3 アーキテクチャパターン
- **レイヤードアーキテクチャ**（階層型アーキテクチャ）
  - API層（Routers）
  - サービス層（Business Logic）
  - データアクセス層（Models）
  - スキーマ層（Validation）

### 1.4 技術スタック
- **Webフレームワーク**: FastAPI 0.109.0
- **データベース**: PostgreSQL 15
- **ORM**: SQLAlchemy 2.0 (async)
- **バリデーション**: Pydantic 2.5.3
- **認証**: JWT (python-jose)
- **パスワードハッシュ**: bcrypt (passlib)
- **Webサーバー**: Uvicorn 0.27.0
- **コンテナ**: Docker, Docker Compose

---

## 2. システムアーキテクチャ

### 2.1 全体構成図

```
┌─────────────────────────────────────────────────────────┐
│                      クライアント                        │
│              (Browser / API Client / CLI)               │
└────────────────────┬────────────────────────────────────┘
                     │ HTTPS/HTTP
                     │
┌────────────────────▼────────────────────────────────────┐
│                   FastAPI Application                   │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │         Middleware Layer                       │    │
│  │  - CORS Middleware                             │    │
│  │  - Exception Handler                           │    │
│  └────────────────┬───────────────────────────────┘    │
│                   │                                      │
│  ┌────────────────▼───────────────────────────────┐    │
│  │         API Layer (Routers)                    │    │
│  │  - auth.py       (認証API)                     │    │
│  │  - users.py      (ユーザーAPI)                 │    │
│  │  - tasks.py      (タスクAPI)                   │    │
│  │  - categories.py (カテゴリAPI - 未実装)        │    │
│  └────────────────┬───────────────────────────────┘    │
│                   │                                      │
│  ┌────────────────▼───────────────────────────────┐    │
│  │         Service Layer                          │    │
│  │  - UserService                                 │    │
│  │  - TaskService                                 │    │
│  │  - CategoryService (未実装)                    │    │
│  └────────────────┬───────────────────────────────┘    │
│                   │                                      │
│  ┌────────────────▼───────────────────────────────┐    │
│  │         Data Access Layer (ORM)                │    │
│  │  - User Model                                  │    │
│  │  - Task Model                                  │    │
│  │  - Category Model                              │    │
│  └────────────────┬───────────────────────────────┘    │
│                   │                                      │
└───────────────────┼──────────────────────────────────────┘
                    │ asyncpg (async driver)
                    │
┌───────────────────▼──────────────────────────────────────┐
│              PostgreSQL Database                         │
│  - users テーブル                                        │
│  - tasks テーブル                                        │
│  - categories テーブル                                   │
└──────────────────────────────────────────────────────────┘
```

### 2.2 ディレクトリ構成

```
fastapi-training-project_1/
├── app/
│   ├── __init__.py
│   ├── main.py                    # アプリケーションエントリーポイント
│   │
│   ├── core/                      # コア機能
│   │   ├── __init__.py
│   │   ├── config.py              # 設定管理（環境変数）
│   │   ├── database.py            # データベース接続・セッション管理
│   │   └── security.py            # セキュリティ（JWT、パスワードハッシュ）
│   │
│   ├── models/                    # データベースモデル（SQLAlchemy）
│   │   ├── __init__.py
│   │   ├── user.py                # Userモデル
│   │   ├── task.py                # Taskモデル
│   │   └── category.py            # Categoryモデル
│   │
│   ├── schemas/                   # Pydanticスキーマ（入出力バリデーション）
│   │   ├── __init__.py
│   │   ├── user.py                # ユーザースキーマ
│   │   ├── task.py                # タスクスキーマ
│   │   └── category.py            # カテゴリスキーマ
│   │
│   ├── api/                       # APIエンドポイント（ルーター）
│   │   ├── __init__.py
│   │   ├── deps.py                # 依存関係（認証、DBセッション取得）
│   │   ├── auth.py                # 認証API
│   │   ├── users.py               # ユーザーAPI
│   │   ├── tasks.py               # タスクAPI
│   │   └── categories.py          # カテゴリAPI（未実装）
│   │
│   └── services/                  # ビジネスロジック層
│       ├── __init__.py
│       ├── user_service.py        # ユーザー操作ロジック
│       ├── task_service.py        # タスク操作ロジック
│       └── category_service.py    # カテゴリ操作ロジック（未実装）
│
├── tests/                         # テストコード
│   ├── __init__.py
│   ├── conftest.py                # pytestフィクスチャ
│   ├── test_auth.py               # 認証テスト
│   ├── test_users.py              # ユーザーテスト
│   ├── test_tasks.py              # タスクテスト
│   └── test_categories.py         # カテゴリテスト（未実装）
│
├── migrations/                    # DBマイグレーション（Alembic - 未使用）
│
├── .env                           # 環境変数（秘匿情報）
├── .env.example                   # 環境変数サンプル
├── requirements.txt               # Python依存関係
├── pyproject.toml                 # プロジェクト設定
├── docker-compose.yml             # Docker構成
├── Dockerfile                     # Dockerイメージ定義
├── Makefile                       # 開発コマンドショートカット
└── README.md                      # プロジェクト説明
```

---

## 3. データベース設計

### 3.1 ER図

```
┌──────────────────────────────┐
│          users               │
├──────────────────────────────┤
│ id (PK)                      │
│ email (UNIQUE, INDEX)        │
│ username (UNIQUE, INDEX)     │
│ hashed_password              │
│ is_active                    │
│ created_at                   │
│ updated_at                   │
└──────────────┬───────────────┘
               │
               │ 1
               │
               │
               │ N
       ┌───────┴────────┬─────────────────┐
       │                │                 │
┌──────▼──────────┐   ┌─▼──────────────┐ │
│    tasks        │   │  categories    │ │
├─────────────────┤   ├────────────────┤ │
│ id (PK)         │   │ id (PK)        │ │
│ title           │   │ name (UNIQUE)  │ │
│ description     │   │ description    │ │
│ is_completed    │   │ color          │ │
│ priority        │   │ user_id (FK)   │ │
│ user_id (FK)    │   │ created_at     │ │
│ created_at      │   │ updated_at     │ │
│ updated_at      │   └────────────────┘ │
└─────────────────┘                      │
                                         │
       ※将来的な拡張として、tasks に     │
         category_id (FK) を追加し、     │
         categories とリレーション構築   │
```

### 3.2 テーブル定義

#### 3.2.1 users テーブル

| カラム名 | データ型 | NULL | デフォルト | 制約 | 説明 |
|---------|---------|------|-----------|------|------|
| id | INTEGER | NOT NULL | AUTO INCREMENT | PRIMARY KEY | ユーザーID |
| email | VARCHAR | NOT NULL | - | UNIQUE, INDEX | メールアドレス |
| username | VARCHAR | NOT NULL | - | UNIQUE, INDEX | ユーザー名 |
| hashed_password | VARCHAR | NOT NULL | - | - | ハッシュ化パスワード |
| is_active | BOOLEAN | NOT NULL | true | - | アカウント有効フラグ |
| created_at | TIMESTAMP | NOT NULL | CURRENT_TIMESTAMP | - | 作成日時 |
| updated_at | TIMESTAMP | NOT NULL | CURRENT_TIMESTAMP | ON UPDATE | 更新日時 |

**インデックス**:
- PRIMARY KEY: `id`
- UNIQUE INDEX: `email`, `username`

#### 3.2.2 tasks テーブル

| カラム名 | データ型 | NULL | デフォルト | 制約 | 説明 |
|---------|---------|------|-----------|------|------|
| id | INTEGER | NOT NULL | AUTO INCREMENT | PRIMARY KEY | タスクID |
| title | VARCHAR(200) | NOT NULL | - | - | タスクタイトル |
| description | TEXT | NULL | NULL | - | タスク説明 |
| is_completed | BOOLEAN | NOT NULL | false | - | 完了フラグ |
| priority | INTEGER | NOT NULL | 1 | CHECK (1-3) | 優先度 |
| user_id | INTEGER | NOT NULL | - | FOREIGN KEY | ユーザーID |
| created_at | TIMESTAMP | NOT NULL | CURRENT_TIMESTAMP | - | 作成日時 |
| updated_at | TIMESTAMP | NOT NULL | CURRENT_TIMESTAMP | ON UPDATE | 更新日時 |

**インデックス**:
- PRIMARY KEY: `id`
- FOREIGN KEY: `user_id` REFERENCES `users(id)`

**リレーション**:
- `users` テーブルと 1:N の関係（1ユーザーが複数タスクを持つ）

#### 3.2.3 categories テーブル

| カラム名 | データ型 | NULL | デフォルト | 制約 | 説明 |
|---------|---------|------|-----------|------|------|
| id | INTEGER | NOT NULL | AUTO INCREMENT | PRIMARY KEY | カテゴリID |
| name | VARCHAR(100) | NOT NULL | - | UNIQUE | カテゴリ名 |
| description | TEXT | NULL | NULL | - | カテゴリ説明 |
| color | VARCHAR(7) | NULL | NULL | - | カラーコード（hex） |
| user_id | INTEGER | NOT NULL | - | FOREIGN KEY | ユーザーID |
| created_at | TIMESTAMP | NOT NULL | CURRENT_TIMESTAMP | - | 作成日時 |
| updated_at | TIMESTAMP | NOT NULL | CURRENT_TIMESTAMP | ON UPDATE | 更新日時 |

**インデックス**:
- PRIMARY KEY: `id`
- UNIQUE INDEX: `name`
- FOREIGN KEY: `user_id` REFERENCES `users(id)`

**リレーション**:
- `users` テーブルと 1:N の関係（1ユーザーが複数カテゴリを持つ）

---

## 4. API設計

### 4.1 認証フロー

```
┌────────────┐                          ┌────────────┐
│  Client    │                          │  Server    │
└──────┬─────┘                          └──────┬─────┘
       │                                       │
       │  POST /api/auth/register              │
       │  {email, username, password}          │
       ├──────────────────────────────────────>│
       │                                       │
       │  201 Created                          │
       │  {id, email, username, ...}           │
       │<──────────────────────────────────────┤
       │                                       │
       │  POST /api/auth/login                 │
       │  {username, password}                 │
       ├──────────────────────────────────────>│
       │                                       │
       │  200 OK                               │
       │  {access_token, token_type}           │
       │<──────────────────────────────────────┤
       │                                       │
       │  GET /api/tasks/                      │
       │  Authorization: Bearer <token>        │
       ├──────────────────────────────────────>│
       │                                       │
       │  200 OK                               │
       │  [{task1}, {task2}, ...]              │
       │<──────────────────────────────────────┤
       │                                       │
```

### 4.2 APIエンドポイント詳細設計

#### 4.2.1 認証API

##### POST /api/auth/register
**概要**: 新規ユーザー登録

**リクエスト**:
```json
{
  "email": "user@example.com",
  "username": "testuser",
  "password": "password123"
}
```

**レスポンス** (201 Created):
```json
{
  "id": 1,
  "email": "user@example.com",
  "username": "testuser",
  "is_active": true,
  "created_at": "2025-11-22T10:00:00",
  "updated_at": "2025-11-22T10:00:00"
}
```

**エラーレスポンス**:
- 400 Bad Request: メールアドレス重複
- 422 Unprocessable Entity: バリデーションエラー

##### POST /api/auth/login
**概要**: ログイン認証

**リクエスト** (OAuth2 PasswordRequestForm):
```
username=user@example.com&password=password123
```

**レスポンス** (200 OK):
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

**エラーレスポンス**:
- 401 Unauthorized: 認証失敗

#### 4.2.2 ユーザーAPI

##### GET /api/users/me
**概要**: 現在のユーザー情報取得

**認証**: 必須

**レスポンス** (200 OK):
```json
{
  "id": 1,
  "email": "user@example.com",
  "username": "testuser",
  "is_active": true,
  "created_at": "2025-11-22T10:00:00",
  "updated_at": "2025-11-22T10:00:00"
}
```

##### PUT /api/users/me
**概要**: 現在のユーザー情報更新

**認証**: 必須

**リクエスト**:
```json
{
  "email": "newemail@example.com",
  "username": "newusername"
}
```

**レスポンス** (200 OK):
```json
{
  "id": 1,
  "email": "newemail@example.com",
  "username": "newusername",
  "is_active": true,
  "created_at": "2025-11-22T10:00:00",
  "updated_at": "2025-11-22T11:00:00"
}
```

##### GET /api/users/{user_id}
**概要**: 指定ユーザー情報取得

**認証**: 必須

**レスポンス** (200 OK):
```json
{
  "id": 1,
  "email": "user@example.com",
  "username": "testuser",
  "is_active": true,
  "created_at": "2025-11-22T10:00:00",
  "updated_at": "2025-11-22T10:00:00"
}
```

**エラーレスポンス**:
- 404 Not Found: ユーザーが存在しない

#### 4.2.3 タスクAPI

##### GET /api/tasks/
**概要**: タスク一覧取得

**認証**: 必須

**クエリパラメータ**:
- `skip`: オフセット（デフォルト: 0）
- `limit`: 取得件数（デフォルト: 100、最大: 100）

**リクエスト例**:
```
GET /api/tasks/?skip=0&limit=10
```

**レスポンス** (200 OK):
```json
[
  {
    "id": 1,
    "title": "タスク1",
    "description": "説明1",
    "is_completed": false,
    "priority": 2,
    "user_id": 1,
    "created_at": "2025-11-22T10:00:00",
    "updated_at": "2025-11-22T10:00:00"
  }
]
```

##### POST /api/tasks/
**概要**: タスク作成

**認証**: 必須

**リクエスト**:
```json
{
  "title": "新しいタスク",
  "description": "タスクの説明",
  "priority": 3
}
```

**レスポンス** (201 Created):
```json
{
  "id": 2,
  "title": "新しいタスク",
  "description": "タスクの説明",
  "is_completed": false,
  "priority": 3,
  "user_id": 1,
  "created_at": "2025-11-22T11:00:00",
  "updated_at": "2025-11-22T11:00:00"
}
```

##### GET /api/tasks/{task_id}
**概要**: タスク詳細取得

**認証**: 必須

**レスポンス** (200 OK):
```json
{
  "id": 1,
  "title": "タスク1",
  "description": "説明1",
  "is_completed": false,
  "priority": 2,
  "user_id": 1,
  "created_at": "2025-11-22T10:00:00",
  "updated_at": "2025-11-22T10:00:00"
}
```

**エラーレスポンス**:
- 404 Not Found: タスクが存在しないか他ユーザーのタスク

##### PUT /api/tasks/{task_id}
**概要**: タスク更新

**認証**: 必須

**リクエスト**:
```json
{
  "title": "更新されたタスク",
  "is_completed": true,
  "priority": 1
}
```

**レスポンス** (200 OK):
```json
{
  "id": 1,
  "title": "更新されたタスク",
  "description": "説明1",
  "is_completed": true,
  "priority": 1,
  "user_id": 1,
  "created_at": "2025-11-22T10:00:00",
  "updated_at": "2025-11-22T12:00:00"
}
```

##### DELETE /api/tasks/{task_id}
**概要**: タスク削除

**認証**: 必須

**レスポンス**: 204 No Content

**エラーレスポンス**:
- 404 Not Found: タスクが存在しないか他ユーザーのタスク

#### 4.2.4 カテゴリAPI（未実装）

##### GET /api/categories/
**概要**: カテゴリ一覧取得（未実装）

**認証**: 必須

**クエリパラメータ**:
- `skip`: オフセット（デフォルト: 0）
- `limit`: 取得件数（デフォルト: 100、最大: 100）

##### POST /api/categories/
**概要**: カテゴリ作成（未実装）

**認証**: 必須

**リクエスト**:
```json
{
  "name": "仕事",
  "description": "仕事関連のタスク",
  "color": "#FF5733"
}
```

##### GET /api/categories/{category_id}
**概要**: カテゴリ詳細取得（未実装）

##### PUT /api/categories/{category_id}
**概要**: カテゴリ更新（未実装）

##### DELETE /api/categories/{category_id}
**概要**: カテゴリ削除（未実装）

---

## 5. セキュリティ設計

### 5.1 認証・認可

#### 5.1.1 JWT認証
- **アルゴリズム**: HS256
- **トークン有効期限**: 30分（デフォルト、環境変数で変更可能）
- **署名鍵**: 環境変数 `SECRET_KEY` で管理
- **ペイロード**:
  ```json
  {
    "sub": "user@example.com",
    "exp": 1700000000
  }
  ```

#### 5.1.2 パスワード管理
- **ハッシュアルゴリズム**: bcrypt
- **ライブラリ**: passlib[bcrypt]
- **平文保存禁止**: パスワードは必ずハッシュ化して保存
- **ソルト**: bcryptが自動生成

#### 5.1.3 認可フロー
1. クライアントがログインリクエスト送信
2. サーバーがパスワード検証（bcrypt verify）
3. 検証成功時にJWT発行
4. クライアントがJWTをAuthorizationヘッダーに含めてリクエスト
5. サーバーがJWT検証（署名、有効期限確認）
6. 検証成功時にリソースへのアクセス許可

### 5.2 CORS設定

**現在の設定** (`app/main.py:29-35`):
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # 全オリジン許可（開発環境のみ）
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**本番環境での推奨設定**:
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["https://example.com"],  # 許可するオリジンのみ
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "DELETE"],
    allow_headers=["Authorization", "Content-Type"],
)
```

### 5.3 セキュリティヘッダー
- 現状は未実装
- 本番環境では以下のヘッダーを推奨:
  - `X-Content-Type-Options: nosniff`
  - `X-Frame-Options: DENY`
  - `Strict-Transport-Security: max-age=31536000`

### 5.4 入力バリデーション
- **Pydanticによる自動バリデーション**:
  - 型チェック
  - 文字列長チェック
  - 範囲チェック
  - 必須項目チェック

### 5.5 SQLインジェクション対策
- **SQLAlchemy ORMの使用**: パラメータバインディングによる自動エスケープ
- **生SQLの禁止**: ORM経由でのみデータアクセス

---

## 6. コンポーネント設計

### 6.1 Core層

#### 6.1.1 config.py
**責務**: アプリケーション設定管理

**クラス**: `Settings`

**主要属性**:
- `APP_NAME`: アプリケーション名
- `APP_VERSION`: バージョン
- `DATABASE_URL`: 本番DB接続URL
- `TEST_DATABASE_URL`: テストDB接続URL
- `SECRET_KEY`: JWT署名鍵
- `ALGORITHM`: JWT署名アルゴリズム
- `ACCESS_TOKEN_EXPIRE_MINUTES`: トークン有効期限

**実装**: `pydantic_settings.BaseSettings` を継承

#### 6.1.2 database.py
**責務**: データベース接続・セッション管理

**主要関数**:
- `get_db()`: 非同期DBセッション取得（依存性注入用）

**主要オブジェクト**:
- `engine`: 非同期エンジン（asyncpg使用）
- `async_session_maker`: セッションファクトリ
- `Base`: SQLAlchemy Declarative Base

**実装詳細**:
```python
engine = create_async_engine(settings.DATABASE_URL, echo=True)
async_session_maker = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
Base = declarative_base()
```

#### 6.1.3 security.py
**責務**: セキュリティ機能（JWT、パスワードハッシュ）

**主要関数**:
- `verify_password(plain_password, hashed_password) -> bool`: パスワード検証
- `get_password_hash(password) -> str`: パスワードハッシュ化
- `create_access_token(data: dict, expires_delta: timedelta) -> str`: JWT生成
- `verify_token(token: str) -> str`: JWT検証

**使用ライブラリ**:
- `passlib.context.CryptContext`: bcryptハッシュ
- `jose.jwt`: JWT生成・検証

### 6.2 Models層

#### 6.2.1 User Model (`app/models/user.py`)
**責務**: ユーザーテーブルのORMマッピング

**主要フィールド**:
- `id`: Integer, Primary Key
- `email`: String, Unique, Indexed
- `username`: String, Unique, Indexed
- `hashed_password`: String
- `is_active`: Boolean
- `created_at`, `updated_at`: DateTime

**リレーション**:
- `tasks`: 1対多（未実装、コメントアウト）

#### 6.2.2 Task Model (`app/models/task.py`)
**責務**: タスクテーブルのORMマッピング

**主要フィールド**:
- `id`: Integer, Primary Key
- `title`: String(200)
- `description`: Text
- `is_completed`: Boolean
- `priority`: Integer (1-3)
- `user_id`: Integer, Foreign Key
- `created_at`, `updated_at`: DateTime

**リレーション**:
- `user`: 多対1（未実装、コメントアウト）

**既知のバグ**:
- `app/models/task.py:24`: リレーションシップがコメントアウトされている

#### 6.2.3 Category Model (`app/models/category.py`)
**責務**: カテゴリテーブルのORMマッピング

**主要フィールド**:
- `id`: Integer, Primary Key
- `name`: String(100), Unique
- `description`: Text
- `color`: String(7)
- `user_id`: Integer, Foreign Key
- `created_at`, `updated_at`: DateTime

### 6.3 Schemas層

#### 6.3.1 User Schemas (`app/schemas/user.py`)

**UserBase**:
- ベーススキーマ
- `email`, `username`

**UserCreate**:
- ユーザー作成スキーマ
- `email`, `username`, `password`

**UserUpdate**:
- ユーザー更新スキーマ
- `email`, `username` (任意)

**UserResponse**:
- レスポンススキーマ
- `id`, `email`, `username`, `is_active`, `created_at`, `updated_at`

**Token**:
- 認証トークンスキーマ
- `access_token`, `token_type`

**TokenData**:
- トークンペイロードスキーマ
- `email`

#### 6.3.2 Task Schemas (`app/schemas/task.py`)

**TaskBase**:
- ベーススキーマ
- `title`, `description`, `priority`

**TaskCreate**:
- タスク作成スキーマ
- TaskBaseを継承

**TaskUpdate**:
- タスク更新スキーマ
- `title`, `description`, `is_completed`, `priority` (全て任意)

**TaskResponse**:
- レスポンススキーマ
- `id`, TaskBase, `is_completed`, `user_id`, `created_at`, `updated_at`

#### 6.3.3 Category Schemas (`app/schemas/category.py`)

**CategoryBase**:
- ベーススキーマ
- `name`, `description`, `color`

**CategoryCreate**:
- カテゴリ作成スキーマ
- CategoryBaseを継承

**CategoryUpdate**:
- カテゴリ更新スキーマ
- `name`, `description`, `color` (全て任意)

**CategoryResponse**:
- レスポンススキーマ
- `id`, CategoryBase, `user_id`, `created_at`, `updated_at`

### 6.4 Services層

#### 6.4.1 UserService (`app/services/user_service.py`)
**責務**: ユーザー関連のビジネスロジック

**主要メソッド**:
- `get_user_by_email(db, email) -> User | None`: メールアドレスでユーザー取得
- `get_user_by_id(db, user_id) -> User | None`: IDでユーザー取得
- `create_user(db, user_data: UserCreate) -> User`: ユーザー作成
- `update_user(db, user_id, user_data: UserUpdate) -> User | None`: ユーザー更新
- `authenticate_user(db, email, password) -> User | None`: 認証

#### 6.4.2 TaskService (`app/services/task_service.py`)
**責務**: タスク関連のビジネスロジック

**主要メソッド**:
- `get_tasks_by_user(db, user_id, skip, limit) -> List[Task]`: ユーザーのタスク一覧取得
- `get_task_by_id(db, task_id, user_id) -> Task | None`: タスク詳細取得
- `create_task(db, task_data: TaskCreate, user_id) -> Task`: タスク作成
- `update_task(db, task_id, task_data: TaskUpdate, user_id) -> Task | None`: タスク更新
- `delete_task(db, task_id, user_id) -> bool`: タスク削除
- `get_completed_tasks_count(db, user_id) -> int`: 完了タスク数取得

**既知のバグ**:
- `app/services/task_service.py:64`: `get_completed_tasks_count` が全タスクをカウントしている

#### 6.4.3 CategoryService (`app/services/category_service.py`)
**責務**: カテゴリ関連のビジネスロジック

**ステータス**: 未実装（学習課題）

**実装予定メソッド**:
- `get_categories_by_user(db, user_id, skip, limit) -> List[Category]`
- `get_category_by_id(db, category_id, user_id) -> Category | None`
- `create_category(db, category_data: CategoryCreate, user_id) -> Category`
- `update_category(db, category_id, category_data: CategoryUpdate, user_id) -> Category | None`
- `delete_category(db, category_id, user_id) -> bool`

### 6.5 API層

#### 6.5.1 deps.py
**責務**: 依存性注入用の共通関数

**主要関数**:
- `get_current_user(token: str, db: AsyncSession) -> User`: JWTからユーザー取得
- `get_current_active_user(current_user: User) -> User`: アクティブユーザー検証

**実装詳細**:
```python
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/auth/login")

async def get_current_user(
    token: str = Depends(oauth2_scheme),
    db: AsyncSession = Depends(get_db)
) -> User:
    # JWT検証 → ユーザー取得
    ...
```

#### 6.5.2 auth.py
**責務**: 認証APIエンドポイント

**エンドポイント**:
- `POST /api/auth/register`: ユーザー登録
- `POST /api/auth/login`: ログイン

#### 6.5.3 users.py
**責務**: ユーザー管理APIエンドポイント

**エンドポイント**:
- `GET /api/users/me`: 現在のユーザー情報取得
- `PUT /api/users/me`: 現在のユーザー情報更新
- `GET /api/users/{user_id}`: 特定ユーザー情報取得

#### 6.5.4 tasks.py
**責務**: タスク管理APIエンドポイント

**エンドポイント**:
- `GET /api/tasks/`: タスク一覧取得
- `POST /api/tasks/`: タスク作成
- `GET /api/tasks/{task_id}`: タスク詳細取得
- `PUT /api/tasks/{task_id}`: タスク更新
- `DELETE /api/tasks/{task_id}`: タスク削除

**未実装機能**:
- タスク統計情報取得
- 優先度フィルター
- タスク検索

#### 6.5.5 categories.py
**責務**: カテゴリ管理APIエンドポイント

**ステータス**: 未実装（学習課題）

---

## 7. データフロー設計

### 7.1 タスク作成フロー

```
[Client]
    │
    │ POST /api/tasks/
    │ {title, description, priority}
    │ Authorization: Bearer <token>
    │
    ▼
[API Layer: tasks.py]
    │
    │ 1. 認証チェック (get_current_active_user)
    │ 2. リクエストバリデーション (TaskCreate)
    │
    ▼
[Service Layer: TaskService.create_task]
    │
    │ 1. タスクオブジェクト生成
    │ 2. user_id設定
    │
    ▼
[Data Access Layer: SQLAlchemy]
    │
    │ 1. INSERT INTO tasks ...
    │ 2. COMMIT
    │ 3. REFRESH (created_atなど取得)
    │
    ▼
[Database: PostgreSQL]
    │
    │ タスクレコード保存
    │
    ▼
[Response]
    │
    │ 201 Created
    │ {id, title, description, is_completed, priority, user_id, created_at, updated_at}
    │
    ▼
[Client]
```

### 7.2 認証フロー詳細

```
[Client]
    │
    │ POST /api/auth/login
    │ {username: "user@example.com", password: "password123"}
    │
    ▼
[API Layer: auth.py]
    │
    │ リクエスト受信
    │
    ▼
[Service Layer: UserService.authenticate_user]
    │
    │ 1. メールアドレスでユーザー検索
    │ 2. パスワード検証 (bcrypt)
    │
    ▼
[Security: security.py]
    │
    │ verify_password(plain, hashed)
    │
    ▼
[Service Layer: 検証結果]
    │
    │ ユーザーオブジェクト返却 or None
    │
    ▼
[API Layer: auth.py]
    │
    │ 1. 検証失敗 → 401 Unauthorized
    │ 2. 検証成功 → JWT生成
    │
    ▼
[Security: security.py]
    │
    │ create_access_token({sub: email}, expires_delta)
    │
    ▼
[Response]
    │
    │ 200 OK
    │ {access_token: "eyJ...", token_type: "bearer"}
    │
    ▼
[Client]
    │
    │ トークン保存
    │
    │ 以降のリクエストで使用
    │ Authorization: Bearer eyJ...
```

---

## 8. 非同期処理設計

### 8.1 非同期処理の採用理由
- **I/O待機時間の削減**: データベースアクセス中に他のリクエストを処理可能
- **スループット向上**: 同時接続数の増加に対応
- **FastAPIの特性活用**: FastAPIは非同期処理をネイティブサポート

### 8.2 非同期処理の実装箇所

#### 8.2.1 データベースアクセス
- **SQLAlchemy 2.0 async**: `AsyncSession`, `create_async_engine`を使用
- **asyncpg**: PostgreSQL非同期ドライバ

```python
# 非同期セッション取得
async def get_db() -> AsyncGenerator[AsyncSession, None]:
    async with async_session_maker() as session:
        yield session

# 非同期クエリ実行
result = await db.execute(select(User).where(User.email == email))
user = result.scalars().first()
```

#### 8.2.2 APIエンドポイント
- すべてのエンドポイント関数に `async def` を使用

```python
@router.get("/tasks/", response_model=List[TaskResponse])
async def get_tasks(
    current_user: User = Depends(get_current_active_user),
    db: AsyncSession = Depends(get_db),
):
    tasks = await TaskService.get_tasks_by_user(db, current_user.id)
    return tasks
```

#### 8.2.3 サービス層
- データベースアクセスを伴うメソッドはすべて `async` / `await`

```python
@staticmethod
async def get_task_by_id(db: AsyncSession, task_id: int, user_id: int) -> Task | None:
    result = await db.execute(
        select(Task).where(Task.id == task_id, Task.user_id == user_id)
    )
    return result.scalars().first()
```

### 8.3 アプリケーションライフサイクル

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    """Application lifespan events"""
    # Startup
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    yield
    # Shutdown
    await engine.dispose()

app = FastAPI(lifespan=lifespan)
```

**Startup時**:
- データベーステーブル自動作成（`create_all`）

**Shutdown時**:
- データベース接続プールの破棄（`dispose`）

---

## 9. テスト設計

### 9.1 テスト戦略
- **ユニットテスト**: サービス層のロジックテスト
- **統合テスト**: API層のエンドポイントテスト（DBを含む）
- **E2Eテスト**: 現状未実装

### 9.2 テスト環境
- **テスト用データベース**: `test_db` (PostgreSQL)
- **TestClient**: FastAPIのテストクライアント（httpx使用）
- **フィクスチャ**: `conftest.py` でDB初期化・テストユーザー作成

### 9.3 テストフィクスチャ（conftest.py）

**主要フィクスチャ**:
- `test_db`: テスト用DBセッション
- `test_client`: FastAPI TestClient
- `test_user`: テスト用ユーザー
- `auth_headers`: 認証ヘッダー（JWTトークン）

**実装例**:
```python
@pytest.fixture
async def test_db():
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    async with async_session_maker() as session:
        yield session
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.drop_all)
```

### 9.4 テストケース設計

#### 9.4.1 認証テスト (`test_auth.py`)
- ユーザー登録成功
- メールアドレス重複エラー
- ログイン成功
- ログイン失敗（パスワード誤り）

#### 9.4.2 ユーザーテスト (`test_users.py`)
- 現在のユーザー情報取得
- ユーザー情報更新
- 特定ユーザー情報取得

#### 9.4.3 タスクテスト (`test_tasks.py`)
- タスク一覧取得
- タスク作成
- タスク詳細取得
- タスク更新
- タスク削除
- 他ユーザーのタスクアクセス拒否

#### 9.4.4 カテゴリテスト (`test_categories.py`)
- **ステータス**: 未実装（学習課題）

---

## 10. 開発環境設計

### 10.1 Docker環境

#### 10.1.1 docker-compose.yml

**サービス構成**:
1. **app**: FastAPIアプリケーション
   - ポート: 8000
   - 依存: db, test_db

2. **db**: PostgreSQL（本番用）
   - ポート: 5432
   - データベース: app_db

3. **test_db**: PostgreSQL（テスト用）
   - ポート: 5433
   - データベース: test_db

#### 10.1.2 Dockerfile

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
```

### 10.2 venv環境

#### 10.2.1 セットアップスクリプト
- **Linux/Mac**: `setup.sh`
- **Windows**: `setup.bat`

**処理内容**:
1. Python仮想環境作成
2. 依存関係インストール
3. 環境変数ファイルコピー

#### 10.2.2 実行スクリプト
- **Linux/Mac**: `run_local.sh`
- **Windows**: `run_local.bat`

**処理内容**:
1. 仮想環境アクティベート
2. Uvicornでアプリケーション起動

### 10.3 環境変数管理

**.env ファイル**:
```env
# Database
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/app_db
TEST_DATABASE_URL=postgresql+asyncpg://user:password@localhost:5433/test_db

# Security
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

---

## 11. 運用設計

### 11.1 ログ設計
- **ログレベル**: INFO（開発環境）
- **ログ出力先**: 標準出力
- **ログフォーマット**: Uvicornデフォルト

### 11.2 監視
- **ヘルスチェック**: `GET /health`
- **メトリクス**: 現状未実装

### 11.3 デプロイ
- 本アプリケーションは開発・学習目的のため、本番デプロイは想定外
- デプロイする場合の推奨構成:
  - Webサーバー: Nginx（リバースプロキシ）
  - アプリサーバー: Uvicorn + Gunicorn
  - データベース: PostgreSQL（マネージドサービス推奨）
  - HTTPS: Let's Encrypt

---

## 12. パフォーマンス設計

### 12.1 データベース最適化
- **インデックス**: email, username にINDEX設定済み
- **コネクションプール**: SQLAlchemyのデフォルト設定を使用
- **N+1問題**: 現状のクエリでは発生しない（リレーション未使用のため）

### 12.2 API最適化
- **ページネーション**: skip/limit パラメータ実装済み
- **レスポンスキャッシュ**: 現状未実装
- **非同期処理**: 全エンドポイントで非同期処理採用

---

## 13. 拡張性設計

### 13.1 将来的な拡張ポイント

#### 13.1.1 カテゴリとタスクのリレーション
- Taskモデルに `category_id` フィールド追加
- Category - Task 間で 1:N のリレーション構築

#### 13.1.2 タスク統計機能
- タスク完了率の算出
- 優先度別タスク数の集計
- カテゴリ別タスク数の集計

#### 13.1.3 検索機能
- タスクタイトル・説明文での全文検索
- 優先度フィルター
- 日付範囲フィルター

#### 13.1.4 ユーザー機能拡張
- プロフィール画像
- ユーザー設定
- 通知機能

### 13.2 マイグレーション戦略
- Alembicを使用したDBマイグレーション
- バージョン管理されたスキーマ変更
- ロールバック対応

---

## 14. 既知の課題・TODO

### 14.1 既知のバグ（学習課題）
1. **タスクモデルのリレーションシップ** (`app/models/task.py:24`)
   - 修正方法: コメントアウトを解除

2. **完了タスク数の集計バグ** (`app/services/task_service.py:64`)
   - 修正方法: `is_completed == True` のフィルター追加

### 14.2 未実装機能（学習課題）
1. カテゴリ管理API（CRUD）
2. タスク統計エンドポイント
3. 優先度フィルター
4. タスク検索機能

### 14.3 改善課題
1. CORS設定の本番対応
2. テストカバレッジ向上
3. エラーハンドリングの統一
4. ログ出力の充実化

---

## 15. 参考資料

- [FastAPI公式ドキュメント](https://fastapi.tiangolo.com/)
- [SQLAlchemy 2.0 ドキュメント](https://docs.sqlalchemy.org/en/20/)
- [Pydantic V2 ドキュメント](https://docs.pydantic.dev/latest/)
- [pytest-asyncio](https://pytest-asyncio.readthedocs.io/)

---

## 16. 変更履歴

| 版数 | 日付 | 変更内容 | 作成者 |
|------|------|----------|--------|
| 1.0 | 2025-11-22 | 初版作成 | - |

---

**文書管理情報**
- 文書番号: DESIGN-001
- 関連文書: REQ-001（要件定義書）
- 承認状態: Draft（ドラフト）
- 最終更新日: 2025-11-22
